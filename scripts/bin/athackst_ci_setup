#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec "$SCRIPT_DIR/ci-bot" setup --source "gh:athackst/ci" --token-file "$HOME/.config/tokens/athackst_ci_bot.token" "$@"
