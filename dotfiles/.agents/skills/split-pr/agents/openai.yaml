interface:
  display_name: "Split PR"
  short_description: "Plan and manage dependency-ordered stacked PR branches."
  default_prompt: "Use $split-pr to create a coherent, easy-to-review stacked PR chain, using hunk-level patch entries when one file belongs in multiple PRs, and verify local branch-level tests."
